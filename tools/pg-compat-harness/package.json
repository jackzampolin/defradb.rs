{
  "name": "pg-compat-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun run src/embedded-test.ts",
    "test:external": "bun run src/run.ts",
    "test:baseline": "PG_URL=postgres://postgres:test@localhost:5432/postgres bun run src/run.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.0",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.7.0"
  }
}
