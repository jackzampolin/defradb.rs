SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
  Docs <- mcDocs
  Blocks <- mcBlocks
  Heads <- mcHeads
  Parents <- mcParents
  Doc <- mcDoc
  InitialDocs = {"docA"}
  LiveDocs = {}
  InitialReceived = {}
  InitialConnected = TRUE
  Mode = "Naive"
INVARIANT TypeOK
INVARIANT INV_TargetMergeClosed
PROPERTY INV_NoLoss
