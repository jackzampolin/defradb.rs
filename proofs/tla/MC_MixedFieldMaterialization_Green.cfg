\* GREEN: field merges update only their component, so a counter merge cannot clobber
\* the LWW field and an LWW merge cannot clobber the counter field.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
  MergeMode = "Componentwise"
INVARIANT INV_TypeOK
INVARIANT INV_Exact
