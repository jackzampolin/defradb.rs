\* GREEN: carve-out's justified domain -- doc scan + unrelated inserts (disjoint keyspace).
\* The suppressed range conflict is a genuine false positive; schedule stays serializable.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
  Txns       <- mcTxns
  Keys       <- mcKeys
  PointReads <- mcPointReads
  RangeKeys  <- mcRangeKeys
  RangeKind  <- mcRangeKind
  Writes     <- mcWrites
  CarveMode = "Correct"
INVARIANT INV_TypeOK
INVARIANT INV_MonotoneCommit
INVARIANT INV_Serializable
