\* Without replacement, two rapid heads for one (document, peer) coexist.
\* Expect: INV_OneLiveHead VIOLATED.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
  Peers <- mcPeers
  MaxHead <- mcMaxHead
  QueueMode = "AppendEvery"
  RetryMode = "CurrentOnly"
INVARIANT INV_TypeOK
INVARIANT INV_OneLiveHead
