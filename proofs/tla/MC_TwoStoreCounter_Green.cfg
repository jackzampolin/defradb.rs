\* GREEN: unified / conflict-checked RMW (Go's single value key, or a per-doc lock
\* spanning local-write + merge). No increment is lost in any interleaving.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
  LocalOps  = {l1, l2}
  RemoteOps = {r1}
  StoreMode = "Unified"
INVARIANT INV_TypeOK
INVARIANT INV_NoLoss
