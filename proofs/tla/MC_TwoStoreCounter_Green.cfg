\* GREEN: unified / conflict-checked RMW (Go's single value key, or a per-doc lock
\* spanning local-write + merge) AND the merged-set dedup guard ON. The materialized
\* value is EXACT in every interleaving — no increment lost, none double-applied.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
  LocalOps  = {l1, l2}
  RemoteOps = {r1}
  StoreMode = "Unified"
  Dedup     = "On"
INVARIANT INV_TypeOK
INVARIANT INV_Exact
