\* RED: DID filtering has the same execution-uniqueness verdict as unfiltered
\* replication when the same-DID contenders remain mutually replicating.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
  Instances <- mcInstances
  DIDs <- mcDIDs
  DidOf <- mcDidOf
  RequestDID <- mcRequestDID
  ClaimRank <- mcClaimRank
  ReplicationPeers <- mcPeersFiltered
INVARIANT TypeOK
INVARIANT INV_ExecutionUnique
