\* Naive: nr never walks ancestry, so it can never satisfy x2's parent x1f (which
\* it filters out) -> stuck (field-grain #2721). INV_VisibleConverge expected to FAIL.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
  Nodes <- mcNodes
  DIDs <- mcDIDs
  DidOf <- mcDidOf
  Blocks <- mcBlocks
  Doc <- mcDoc
  Parents <- mcParents
  Heads <- mcHeads
  OwnerWrite <- mcOwnerWrite
  CreateOwner <- mcCreateOwner
  RelRef <- mcRelRef
  FilteredBlocks <- mcFiltered
  Creator = "nx"
  FilterScope = "SubDoc"
  KeyMutability = "Immutable"
  FetchPolicy = "Naive"
INVARIANT TypeOK
PROPERTY INV_VisibleConverge
