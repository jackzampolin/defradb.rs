\* RED: a field merge commits a stale whole-document snapshot. A write to the other
\* field between snapshot and commit is lost, violating the mixed exact-state oracle.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
  MergeMode = "WholeDoc"
INVARIANT INV_TypeOK
INVARIANT INV_Exact
